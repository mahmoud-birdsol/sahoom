<div class="flex items-start justify-between gap-4">
    <div class="space-y-0.5">
        <flux:heading size="xl" class="font-bold text-zinc-900">{{ __('Overview') }}</flux:heading>
        <flux:subheading class="text-zinc-500">{{ __('Welcome back, manage your properties and bookings') }}</flux:subheading>
    </div>

    <flux:button href="#" variant="primary" icon="plus" class="shrink-0 bg-amber-500 hover:bg-amber-600 border-amber-500 hover:border-amber-600">
        {{ __('New Property') }}
    </flux:button>
</div>
